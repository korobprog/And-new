{
  "name": "vite-starter",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "jest",
    "gh-deploy": "git push origin :gh-pages && git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "jest": "^27.4.5",
    "sass": "^1.43.5",
    "vite": "^2.6.13"
  },
  "dependencies": {
    "gh-pages": "^6.0.0",
    "lil-gui": "^0.15.0",
    "three": "^0.135.0"
  }
}
